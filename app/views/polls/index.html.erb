<h1>Polls</h1>

<ul>
  <% @polls.each do |poll| %>
    <li>
      <%= link_to poll.title, poll %>
    </li>
  <% end %>
</ul>

<% if user_signed_in? %>
  <% if current_user.admin %>
    <%= link_to "New Poll", new_poll_path %>
  <% end %>
<% end %>